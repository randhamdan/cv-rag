

fastapi(backend)
streamlit(frontend)
pinecone(vector database)
langchain
sentence-transformers

#project arch
streamlit frontend
fastapi backend
langChain
pinecone(vector database)

setup
create enviroment
activate env
install dependecies

env variable
create env file and add 
pinecone-api-key
openrouter-api-key

run fastapibackend:uvicorn main:app --reload
run streamlit frontend:streamlit run app-py

